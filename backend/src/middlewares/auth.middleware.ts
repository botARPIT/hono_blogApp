
// import { generateAccessToken, generateRefreshToken } from "../utils/jwt";

// export function signInMiddleware(email:string, accessKey :string, refreshKey: string) {
//        const accessToken =  generateAccessToken(email, accessKey);
//        const refreshToken =  generateRefreshToken(email, refreshKey);
//         return {accessToken, refreshToken}
// }

